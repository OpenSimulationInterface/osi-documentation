The validator takes as an argument a dictionary were the rules for a validator
are stored. 

# Dictionary with OSI Rules
* Files with OSI Rules start with osi_ and end with `.yml`.
* All the files in a directory will be scanned
